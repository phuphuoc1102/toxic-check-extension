import React from "react";
import { Outlet } from "react-router-dom";

const SettingTab = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
};

export default SettingTab;