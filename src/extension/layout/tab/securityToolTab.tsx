import React from "react";
import { Outlet } from "react-router-dom";

const SecurityToolTab = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
};

export default SecurityToolTab;