import React from 'react';

const Spinner: React.FC = () => (
  <div className="flex justify-center items-center">
    <div className="spinner"></div> {/* CSS cho spinner */}
  </div>
);

export default Spinner;
